apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Key }}
  namespace: {{ .Namespace }}
spec:
  dnsNames:
  - {{ .FQDN | quote }}
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt
  secretName: {{ .Key }}